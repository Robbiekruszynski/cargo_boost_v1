// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    // Define other color aspects if needed
  },
  // Add other theme customizations if needed
});

export default theme;
